了解しました。QRコード描画において、市松模様（2セル四方単位）でURL1とURL2のセルを交互に配置する新しい分割パターンを追加する方法について調査します。

既存の垂直・水平・対角線に加えて、市松（チェッカーボード）パターンを追加するためのコードの変更方法、実装上の注意点、UI上の選択肢追加についても含めて詳しく説明します。


# React QRコードアプリへのチェッカーボード分割パターン追加方法

既存のQRコード生成アプリ（App.jsx）に4つ目の分割パターン「市松模様（チェッカーボード）」を追加する方法について説明します。これにより、2つのURLを1つのQRコードにエンコードする際、**2×2セルのブロック単位**でURL1とURL2の情報を交互に配置するパターンが選択可能になります。以下に、UIの変更点と`generateDualQRCode`関数内の描画ロジック変更、およびチェッカーボードパターンの判定ロジックと既存パターンとの統合方法を詳述します。

## UIの変更点：チェッカーボードパターン選択肢の追加

まず、ユーザーがチェッカーボードパターンを選択できるようにUIを変更します。既に垂直・水平・対角線の3種類のパターン切替が実装されているはずなので、そこに\*\*「チェッカーボード」\*\*の選択肢を追加します。実装上は、ラジオボタンやセレクトボックスのオプションを1つ増やす形です。例えばラジオボタンで実装している場合、以下のようなJSXを追加します。

```jsx
<div>
  {/* 既存の他パターンのラジオボタン ... */}
  <label>
    <input 
      type="radio" 
      name="pattern" 
      value="checkerboard" 
      checked={pattern === 'checkerboard'} 
      onChange={e => setPattern(e.target.value)} 
    />
    市松模様（チェッカーボード）
  </label>
</div>
```

上記では、状態変数`pattern`（選択中のパターン種別）に`'checkerboard'`をセットするラジオボタンを追加しています。セレクトボックスを用いている場合も同様に、`<option value="checkerboard">チェッカーボード</option>`を追加してください。**重要なのは**、他の3パターンと同じ扱いでチェッカーボードパターンを選択可能にすることです。UI変更後は、ユーザーがこの新パターンを選ぶと`pattern`状態が`'checkerboard'`になり、新しい描画ロジックが適用されます。

## generateDualQRCode関数の変更点：チェッカーパターン描画ロジック

次に、QRコードの描画処理を行っている`generateDualQRCode`関数内に、チェッカーボードパターン用の処理を追加します。既存では垂直(`vertical`)・水平(`horizontal`)・対角線(`diagonal`)の場合に応じて、**各セル内で**URL1とURL2のビットが異なるときにセルを半分に分割して塗り分けているはずです。チェッカーボードパターンでは、\*\*各「曖昧な」セル（URL1とURL2でビットが異なるセル）内を4分割（2×2の小さな正方形ブロック）\*\*し、市松模様状に交互の色で塗り分けます。具体的なコード変更イメージは以下のようになります。

```jsx
function generateDualQRCode() {
  // （前略）QRコードデータ生成部分
  
  const cellSize = /* 1セルを描画するピクセルサイズ */;
  for (let row = 0; row < qrSize; row++) {
    for (let col = 0; col < qrSize; col++) {
      const bit1 = qrData1[row][col];  // URL1のQRコード上のビット（true/falseや1/0で黒白を示す）
      const bit2 = qrData2[row][col];  // URL2のビット
      
      if (bit1 === bit2) {
        // 両URLで同じ値なら、そのまま塗りつぶし（黒なら黒、白なら白）
        ctx.fillStyle = bit1 ? '#000' : '#fff';
        ctx.fillRect(col * cellSize, row * cellSize, cellSize, cellSize);
      } else {
        // 両URLでビットが異なる（曖昧な）セルの場合
        if (pattern === 'vertical') {
          // 垂直分割: 左半分と右半分を別々の色で描画
          // 例: URL1が黒・URL2が白なら左を黒、右を白に塗る
        } else if (pattern === 'horizontal') {
          // 水平分割: 上半分と下半分を塗り分け
        } else if (pattern === 'diagonal') {
          // 対角線分割: 斜めに二分割して塗り分け
        } else if (pattern === 'checkerboard') {
          // チェッカーボード分割: セル内を4つの小ブロックに塗り分ける
          const color1 = bit1 ? '#000' : '#fff';  // URL1側の色
          const color2 = bit2 ? '#000' : '#fff';  // URL2側の色
          const half = cellSize / 2;
          
          // セル内を2x2に分割し、それぞれ塗りつぶす
          // top-left サブセル
          ctx.fillStyle = color1;
          ctx.fillRect(col * cellSize, row * cellSize, half, half);
          // top-right サブセル
          ctx.fillStyle = color2;
          ctx.fillRect(col * cellSize + half, row * cellSize, half, half);
          // bottom-left サブセル
          ctx.fillStyle = color2;
          ctx.fillRect(col * cellSize, row * cellSize + half, half, half);
          // bottom-right サブセル
          ctx.fillStyle = color1;
          ctx.fillRect(col * cellSize + half, row * cellSize + half, half, half);
        }
      }
    }
  }
  
  // （後略）生成したキャンバスを表示する処理など
}
```

上記はチェッカーボードパターンの**基本的な描画方法の例**です。`pattern === 'checkerboard'`の場合にのみ、セルを4分割して塗り分けています。この例では、`bit1`が黒（true）で`bit2`が白（false）の場合、\*\*左上と右下の小ブロックを黒（URL1の色）、右上と左下の小ブロックを白（URL2の色）\*\*に塗る実装になっています（逆の場合は配色も反転します）。こうすることで1つのセル内に市松模様状の2色を配置できます。

> **補足:** 上記コードでは、単純に各曖昧セルを同じパターン（左上・右下がURL1、他がURL2）で塗り分けています。これでも市松模様状になりますが、2×2セルのブロック単位でURL1とURL2を交互配置するには、ブロックごとにこのパターンを反転させるとより**盤面状の市松模様**になります（詳細は後述）。

## チェッカーボードパターンの判定ロジック

チェッカーボードパターンでは、「各小ブロックに対してURL1とURL2のどちらの情報（色）を使うか」を決める判定ロジックが必要です。基本的なアイディアは、**2×2の小セルで交互に色を配置する**ことです。先ほどのコード例では、各曖昧セル内で**固定的に**「左上・右下をURL1、右上・左下をURL2」としました。しかし、2×2セルのブロック単位で交互配置するには、**ブロックごとにどちらのURLの色を左上にするかを交互に切り替える**と見た目が市松模様状に整います。

判定方法の一例として、**セル座標を用いた条件式**を使う方法があります。例えば:

```jsx
// row, col は現在のセルの行・列インデックス
const useUrl1 = ((Math.floor(row / 2) + Math.floor(col / 2)) % 2 === 0);
```

この例では、QRコード上のセルを2×2のブロックに分け、ブロックごとに`useUrl1`フラグが真偽で切り替わります。`Math.floor(row/2)`と`Math.floor(col/2)`でそれぞれ**現在のセルが属する2×2ブロックの位置**を取得し、その和の偶奇で判定しています。`useUrl1`が`true`の場合はそのセル内の**市松模様パターンの左上をURL1の色**にし、`false`なら左上をURL2の色にする、といった具合に使います。

さらにセル内の\*\*小ブロック（2×2のサブセル）\*\*で交互に色を配置するには、サブセル内のインデックスを用いて判定します。例えば、サブセル内の行・列を`subRow`・`subCol`（それぞれ0または1）とすると、`(subRow + subCol) % 2`の結果で交互を判断可能です。この値が0なら一方の色、1ならもう一方の色を塗るようにすれば、**左上・右下と右上・左下で色が異なるチェッカーパターン**が実現します。具体的には次のような論理になります。

```jsx
for (let subRow = 0; subRow < 2; subRow++) {
  for (let subCol = 0; subCol < 2; subCol++) {
    // (subRow + subCol) % 2 が0なら左上・右下と同じパリティ
    const subCellParityEven = ((subRow + subCol) % 2 === 0);
    let fillColor;
    if (useUrl1) {
      // ブロック基準でURL1が「先手」の場合
      fillColor = subCellParityEven ? color1 : color2;
    } else {
      // ブロック基準でURL2が先手の場合（パターン反転）
      fillColor = subCellParityEven ? color2 : color1;
    }
    ctx.fillStyle = fillColor;
    ctx.fillRect(col * cellSize + subCol * half, row * cellSize + subRow * half, half, half);
  }
}
```

上記では、まず`useUrl1`によって**そのセル（属するブロック）のパターンの基準色**を決め、`subRow + subCol`の偶奇によって各小ブロックを交互に塗り分けています。結果として、2×2のセルブロックごとに**交互に基準が切り替わる**市松模様パターンが完成します。例えば:

* ブロックの左上セルでは`useUrl1`が真（URL1が基準）なら、そのセル内は左上・右下をURL1色、右上・左下をURL2色に。
* 隣のブロック（例えば右隣の2×2セルブロック）では`useUrl1`が偽となり、パターンが反転して左上・右下をURL2色、他をURL1色に。

このように**ブロックごとに基準を反転**させることで、全体として大きな市松模様（チェッカーボード）のようにURL1とURL2の情報が配置されます。

**判定ロジックまとめ:**

* 各セルについて `(Math.floor(row/2) + Math.floor(col/2)) % 2` を計算し、偶数ならそのブロックでは「URL1を基準パターン」、奇数なら「URL2を基準パターン」に設定。
* 各曖昧セル内をさらに2×2に分割し、`(subRow + subCol) % 2` によって交互に色を決定。基準がURL1の場合、`(subRow + subCol) % 2 == 0`の小ブロックはURL1の色、`== 1`の小ブロックはURL2の色。基準がURL2の場合はその逆にする。

この判定により、「2×2セルのブロック単位でURL1とURL2の情報を交互に配置する」動作を実現できます。

## 他の描画パターンとの統合と注意点

既存の垂直・水平・対角線パターンのロジックを壊さずにチェッカーボードパターンを追加するには、**条件分岐を適切に拡張する**ことが重要です。基本的には以下のポイントに留意して実装します。

* **既存コードの分岐に新ケースを追加:** 前述のように、`pattern`の値によって処理を切り替えている部分に`else if (pattern === 'checkerboard') { ... }`ブロックを追加します。他のパターン分岐には手を加えず、新しい分岐を挿入することで既存の挙動を維持します。
* **既存設定との整合性:** チェッカーボードパターン追加によって、他の設定（QRコードのバージョン、エラー訂正レベル、セルサイズなど）に影響が出ないようにします。基本的には描画方法の違いだけなので、既存ロジックをそのまま利用しつつ描画部分だけ差し替えれば問題ありません。
* **描画結果の確認:** 他のパターン同様に、新パターンでも**見た目が崩れないこと**、および**QRコードのスキャン結果に影響を与えないこと**を確認します。特にチェッカーボードパターンでは非常に細かい分割となるため、セルサイズが極端に小さいとスキャナーに認識されにくくなる可能性があります。必要に応じてセルサイズ（ピクセル数）を調整してください。また、ファインダー（位置検出パターン）など**QRコードの機能パターン部分は通常どおり単色で描画**し、分割処理の対象から除外するよう注意します（既存実装でも同様の処理がある場合があります）。

以上のように実装することで、既存の3種のパターンに加えて4つ目の「チェッカーボード」パターンを安全に組み込むことができます。UIでユーザーがチェッカーパターンを選択すると、`generateDualQRCode`内で新しいロジックが働き、**各曖昧セルが市松模様状に塗り分けられたデュアルQRコード**が生成されます。これにより、見た目にもチェッカーボード風のパターンが加わりつつ、複数のURLを1つのQRコードで表現する仕組みが拡張されます。
