承知しました。目標のQRコード（2枚目）に近い描画スタイルを分析し、現在のコードとの差異を特定して、読み取り可能性の高い checker-symmetric スタイルの最適な描画ロジックを提案します。

具体的には、セルの左右分割方法、分割位置の調整、checker配置のパターン、および読み取り性能を最大化するための対策を含めて、修正コードの形でお伝えします。


# QRコード「checker-symmetric」分割方式の改良提案

## 現状の実装と課題

現在の**checker-symmetric**方式では、各QRコードのモジュール（セル）を左右2分割し、左半分にURL1、右半分にURL2のパターンを配置しています。また、セル位置の奇偶（`(row + col) % 2`）によって左右の分割パターンを交互に切り替えているとのことです。これにより、隣接するセルで左右の役割を入れ替えるチェッカーボード状の配置になります。しかし、この方法には**読み取りの安定性**という面でいくつかの課題があります。

1. **センターサンプリングの問題:** 多くのQRコードリーダーは各モジュールの中心付近の色をサンプリングしてデコードします。現在の実装では各セルをちょうど中央で二分割しているため、スキャナから見て真正面（傾きなし）だと**モジュール中心がちょうど境界線上**に位置してしまいます。これではセル中央が黒半分と白半分の境界に当たり、どちらのコードの色と認識されるか不安定になります。その結果、読み取り角度によっては両URLの情報が混在し、**デコードエラーや特定のURLへの偏り**が生じる可能性があります。

2. **交互パターンによる一貫性の欠如:** `(row+col)%2`で左右の役割を切り替える現在のチェッカーパターンでは、あるセルでは左側がURL1の情報、隣のセルでは左側がURL2の情報というように、**どのURLの色がセル中央付近に来るかがセル毎に異なる**構造になっています。これだと、例えば真正面から読み取った場合に、あるセルではURL1のパターンを読み込もうとし別のセルではURL2のパターンを読み込もうとするため、単一のQRコードとして整合したデータになりません。エラー訂正機能によりある程度の欠損は補正できるものの、モジュールごとに主従が入れ替わる構造では**どちらのQRコードも完全には復元しづらい**状態になります。

3. **境界（機能パターン）の処理:** QRコードには位置検出パターンやタイミングパターン、フォーマット情報など、読み取りに不可欠な**機能パターン**が含まれています。2つのQRコードを組み合わせる際に、これら**重要領域が両コードで異なる場合**まで安易に左右分割してしまうと、コード自体の検出・位置合わせが不安定になります。特に位置検出パターン（大きな3つの隅の○模様）は両URLで同一である必要があります。この部分を分割するとQRコードとして認識されなくなる恐れがあります。現状の実装でも同一パターンの場合はそのまま塗りつぶす処理になっていますが、万一機能パターンに差異が出た場合の扱いが明確でないと読み取り精度に影響します。

4. **奇数ピクセルのセルサイズ:** 描画上のセルサイズ(`cellSize`)が奇数の場合、現在の実装では**境界線がピクセルの中央を通る**ことになります。例えばセルを5ピクセル幅で描画する場合、左右2分割だと2.5ピクセルずつという計算になり実際には2pxと3pxに分ける必要があります。この際の**1ピクセルの差の扱い**を統一しないと、セル境界にずれや濃淡が生じたり、中心サンプリング時に予期しない色を拾ったりしかねません。現在のコードでこの処理が明示されていない場合、偶然片側に余分なピクセルが寄ってしまい、全体のパターンに歪みを生じさせている可能性があります。

以上の理由から、現状のchecker-symmetric方式では読み取りにムラが生じ、特に角度を付けない通常スキャン時に**特定のURLに偏ってデコードされる**傾向（例：2つのうち片方のURLばかり検出される）が報告されています。

## 目標のQRコードとの違い

ご提示の「目標のQRコード」（参考画像）のほうが安定して読み取れるとのことですが、その背景には**モジュール分割構造と描画方法の工夫**があります。主な違いは次のとおりです。

* **上下左右への対称分割（チェッカーボード状の細分化）:** 目標QRでは、異なるパターンを持つセルを縦横両方向に二分割し、1つのセル内に4つの小領域（**2×2のミニ格子**）を作っています。具体的には、セル内でURL1の色を塗る小マスとURL2の色を塗る小マスを斜めに配置したチェッカーボード状にすることで、各セルの中心近傍が**必ずどちらか一方の色に属する**よう工夫されています（セル中心を十字で分割すると、その中心点は4分割の交点になりますが、後述するピクセルオフセットにより実質どちらかの色領域に寄るようにしています）。この\*\*「微細チェッカーパターン」\*\*により、QRコードリーダーの角度依存のサンプリングに対して、片方のコード情報が明確に読み取れる割合を高めています。

* **一貫したパターン配置（対角の統一）:** 目標コードでは、おそらく**全セルで一貫した小領域配置**を用いています。例えば「URL1のパターン色をセル内の左上・右下の小領域に配置し、URL2を右上・左下に配置する」といった具合に、すべての異なるセルで統一した配置パターンを適用します。こうすることで、例えば真正面から見たとき**常にURL1側の色が各セル中心に近い領域を占める**ようになり、URL1のQRコードとして整合したパターンが現れます（URL2側はその逆の角度で現れる）。現状実装のようにセルごとにパターンを反転させていないため、**各QRコードのデータ構造がセル全域で首尾一貫して保持**され、エラー訂正なしでも片方のコードを認識しやすくなっています。

* **機能パターンの維持:** 目標コードでは**QRコードの機能パターン（位置検出マーク、タイミングパターン、フォーマット情報領域など）を損なわない描画**が行われているはずです。具体的には、これら領域は2つのQRコードで差異が出ないようあらかじめ調整するか、差異が出た場合でも一方のコードの値を優先して**分割せずそのまま描画**していると考えられます。特に位置検出パターンは両コード共通の黒ブロックになるよう生成し、そこは二重化しない（片方に統一する）ことで**傾きに依存せず検出できる安定したマーカー**となります。このように「重要な機能セルは二分割せず**保持**し、**データセルのみ**を分割する」というポリシーによって、角度を付けてもコード自体の認識（土台部分）は揺らがないようにしています。

* **分割位置とピクセル精度の調整:** 目標QRではセル内の分割位置が厳密に調整されています。ただ真ん中で線を引くだけでなく、**分割線を僅かにズラす**ことで片側領域を僅かに大きくしています。例えばセルを10ピクセル角で描画する場合、縦横とも中央の5pxではなく4px:6pxや6px:4pxに分けるなど、中心点が必ずどちらかの領域内に収まるよう工夫します（**ハーフピクセル手法**）。特にセルサイズが奇数の場合は自動的にどちらかが1ピクセル大きくなりますが、偶数サイズの場合でも敢えて1ピクセル差をつけることで**完全正面でも主となるコードが決まる**状態にします。この微調整によって、リーダーが真正面から読み取る際には常に特定のURL側の色を中心として検出し、一方のURLは**カメラを傾けたときにのみ現れる**ようになります。結果として、読み取り角度によらず常にどちらか片方（通常はデフォルトの片方）のURLは確実に復元できる状態を実現しています。

* **モジュールサイズの最適化:** 加えて、目標QRコードでは各セル（モジュール）のピクセルサイズにも配慮があるかもしれません。研究事例では、**1モジュールあたり約9～11ピクセル程度**の解像度が角度依存エンコードを安定動作させる目安とされています。十分なピクセル数がないと、細分化したセル内のパターンがカメラに潰れて認識されない可能性があります。おそらく目標コードではセルサイズを大きめに（またはDPIを上げて）設定し、斜めから見てもミニ格子パターンが明確に判別できるよう最適化されているでしょう。

以上の構造的違いにより、目標のQRコードは\*\*「どの角度からスキャンしても少なくとも一方のメッセージを確実に読み取れる」\*\*安定性を実現しています。では、このアプローチを踏まえて現行実装をどのように修正・最適化するかを具体的に提案します。

## 改良された checker-symmetric アルゴリズム

提案する新しい**checker-symmetric**描画アルゴリズムでは、各モジュールをより精巧なチェッカー（市松模様）状に分割し、読み取り精度の向上を図ります。具体的な手順は以下のとおりです。

* **① 同一パターンセルの保持:** まず、基本は既存実装と同様です。2つのQRコードで同じ値（両方黒または両方白）になっているセルについては分割せず**完全に塗り潰します**。この場合はどちらのURLの情報においても相違がない箇所なので、単一色（黒なら黒、白なら白）でそのセルを描画します。こうすることで、元のQRコード両方のパターンをそのまま反映できます。

* **② 相違セルの細分化:** 2つのコードで**パターンが異なるセル**（片方が黒、もう片方が白）については、従来は左右に半分だけ分割していましたが、これを\*\*上下左右4分割（チェッカー型）\*\*に変更します。セル内に垂直・水平の両方の仕切り線を引き、1つのセルを4つの小領域（quadrants）に区切ります。例えばセルが `cellSize` ピクセルの正方形なら、概ね縦横それぞれ`cellSize/2`ピクセルずつの領域になります（奇数の場合は後述のとおり片側が1ピクセル大きい）。

* **③ 対角領域への色配置:** 4分割した各セル内で、**対角に位置する小領域どうしを同じ色で塗りつぶします**。一方の対角（二つの小マス）にはURL1のQRコードが要求する色を、もう一方の対角にはURL2のQRコードが要求する色を割り当てます。例えば、そのセルにおいてURL1側が黒・URL2側が白である場合、セル左上と右下の小領域を黒（URL1の色）に、右上と左下の小領域を白（URL2の色）にします（下図イメージ）。逆にURL1が白・URL2が黒のセルでは、左上と右下を白、右上と左下を黒で塗ります。この配置によりセル全体としては市松模様状になり、**どの方向から見ても必ず半分の面積が黒・半分が白**という構造になります（ただし中心位置の占め方は後述のオフセットで調整します）。

* **④ パターン配置の一貫性:** **全セルで上記の対角配置パターンを統一します。**重要なのは、セルごとに対角の色配置をコロコロ反転させないことです。提案アルゴリズムでは、全ての異なるセルにおいて「左上＆右下=URL1の色、右上＆左下=URL2の色」というルールで塗り分けます（この例ではURL1のパターン色を常に左上小領域に置くケース）[^1]。こうすることで、たとえばデフォルトでは**URL1のコードが全面的に優先されたパターン**が浮かび上がります。リーダーが真正面からスキャンした場合、各セルの中心近傍は常にURL1側の色領域となるため、結果としてURL1のQRコードを比較的容易に復元できます。一方、カメラを傾けるなどして読み取ると中心サンプリング位置がセル内でずれ、代わりにURL2側の色領域が中心に来るため、URL2のコードが読み取れるようになります。このようにセル内パターンを統一しておけば、**角度ごとにどちらか一方のコードがモジュール全域で優位**になるため、安定したデコードが期待できます（片方を読む際はもう片方の情報はエラー訂正で吸収可能）。逆に以前のようにセル単位で左右反転を繰り返すと、一つのQRコードとしてまとまったパターンが出現しにくくなるため避けます。

* **⑤ 分割位置のオフセット調整:** 縦横に引く分割線は**中央でなく、わずかに片側に寄せます**。これにより、モジュール中心点が特定の色領域側に入るよう微調整します。実装上は、`cellSize`を半分に割る際に整数演算で端数処理することで達成できます。例えば`cellSize`ピクセルを二分割する場合、通常は `half = Math.floor(cellSize/2)` とし、もう一方を `cellSize - half` とすれば偶数・奇数両方に対応できます。**奇数サイズの場合**は一方が他方より1ピクセル大きくなります。**偶数サイズの場合**でも、あえて片側を+1ピクセル大きく・他方を-1ピクセル小さくすることで1ピクセルの非対称を作ることも可能です（総和はcellSizeに等しいよう調整）。この1ピクセル差が「ハーフピクセル」効果を生み、**セル中心が境界線ではなく特定の色領域内に位置する**ようになります。どちら側に寄せるかは、優先したいURL側の色領域を大きく取るように決めます。たとえばURL1をデフォルトで読み取りやすくしたいなら、上記のように「左上＆右下をURL1色」にした上で、分割線を**左および上側領域が1ピクセル大きくなる方向**にオフセットします。具体的には、縦横とも `half1 = Math.floor(cellSize/2) + 1`（URL1側領域）、`half2 = cellSize - half1`（URL2側領域）といった計算で、左上（URL1側）の小領域が他の3領域より一回り大きくなるようにします。こうするとセル中心点は自然と左上領域（URL1色）の中に収まり、真正面ではURL1の模様を正しく取得しやすくなります。一方URL2を読みたい場合はカメラを傾けて中心を別領域に移す必要がある、という明確な切り分けができます。逆にURL2をデフォルト優先にしたければ、分割線を右下側（URL2側）が大きくなるよう設定すればOKです。このように**セル内の境界線位置を50:50からずらすことで中心サンプルの色を制御**し、読み取り結果を安定化させます。

* **⑥ 重要モジュールの扱い:** 位置検出パターンやタイミングパターン、誤り訂正レベル・マスクを示すフォーマット情報セルなど、QRコードの機能に関わるモジュールについては**可能な限り分割を避けます**。幸い、通常2つのURLから生成するQRコードは同じバージョン・誤り訂正レベルを選べば、こうした機能セルの大部分は一致します。位置検出マーク（隅の3つの大きなブロック）は必ず黒固定ですし、タイミングパターンも各セル交互に黒白が並ぶ規則は両方で同じです。そのためこれらは「両コードで同一パターン」と判定され**そのまま塗り潰し**になります（分割されません）。ただし例えばマスクパターンの違い等でフォーマット情報領域の一部などに差異が出た場合は、安定性を優先し**片方のコードの値に合わせてしまう**ほうが安全です。フォーマット情報やバージョン情報はQRコード全体の誤り訂正とは別に数ビットの誤り訂正符号を持つため、多少片方と異なっても一方が正しければ読み取りは可能です。従って、これら**機能領域は無理に2分割せず一方に揃える**（実装上は「差異があってもsplitせずURL1側の値で塗る」等）処理を入れると、より堅牢になるでしょう。

[^1]: ※セルごとのパターン交互切替えは基本的に不要です。どうしても全体のデザイン上交互にしたい場合（例えば大きなブロックノイズ回避など）のみ `(row+col)%2` を使って対角パターン自体を反転させることも考えられますが、その場合でも**中心オフセットと主従の一貫性**は崩さないよう注意が必要です（こちらは高度な調整になるため、まずは全セル同一パターンで問題ありません）。

以上がアルゴリズムのポイントです。要約すると、「**各異なるセルを上下左右4分割し対角色配置にする**」「**分割線をわずかにずらして中心点を片方の色領域に含める**」「**機能パターンは極力そのまま保持する**」の3点が読み取り精度向上の肝となります。

## 修正すべきコードの例

上記アルゴリズムを既存の`App.jsx`のキャンバス描画ループに統合するコード例を示します。現在の実装では`pattern`に応じて **vertical** / **horizontal** / **diagonal** の各パターンで描画処理を切り替えていると思われますので、新たに **checker-symmetric** の分岐を追加します。以下のコードスニペットでは、各セルについて2つのQRコードのビット値を取得し、同一パターンか異なるかで描画を分けています。コメントとともに修正内容を詳述します。

```javascript
// 前提: ctx はCanvas 2Dコンテキスト、qr1Modulesとqr2ModulesはそれぞれのQRコードのビット配列
//       cellSizeはセル一辺のピクセルサイズ、offsetX/offsetYは描画開始オフセット
for (let r = 0; r < qr1Modules.length; r++) {
  for (let c = 0; c < qr1Modules[r].length; c++) {
    const bit1 = qr1Modules[r][c];  // URL1の該当セル (true=黒、false=白)
    const bit2 = qr2Modules[r][c];  // URL2の該当セル
    const x = offsetX + c * cellSize;
    const y = offsetY + r * cellSize;
    if (bit1 === bit2) {
      // 両コードで同じパターンの場合 -> その色でセル全体を塗り潰す
      ctx.fillStyle = bit1 ? "#000" : "#FFF";
      ctx.fillRect(x, y, cellSize, cellSize);
    } else {
      // 両コードでパターンが異なる（片方黒・片方白）の場合 -> パターンに応じて分割描画
      switch (pattern) {
        case "vertical":
          // 垂直2分割パターン（既存実装例）
          {
            const halfW1 = Math.floor(cellSize / 2);      // 左側の幅
            const halfW2 = cellSize - halfW1;             // 右側の幅（halfW1と合計してcellSize）
            // ここでは例として左半分をURL1、右半分をURL2の色で塗る
            ctx.fillStyle = bit1 ? "#000" : "#FFF";       // URL1の色
            ctx.fillRect(x, y, halfW1, cellSize);         // 左半分描画
            ctx.fillStyle = bit2 ? "#000" : "#FFF";       // URL2の色
            ctx.fillRect(x + halfW1, y, halfW2, cellSize);// 右半分描画
          }
          break;
        case "horizontal":
          // 水平2分割パターン（既存実装例）
          {
            const halfH1 = Math.floor(cellSize / 2);      // 上側の高さ
            const halfH2 = cellSize - halfH1;             // 下側の高さ
            ctx.fillStyle = bit1 ? "#000" : "#FFF";       // URL1の色
            ctx.fillRect(x, y, cellSize, halfH1);         // 上半分描画
            ctx.fillStyle = bit2 ? "#000" : "#FFF";       // URL2の色
            ctx.fillRect(x, y + halfH1, cellSize, halfH2);// 下半分描画
          }
          break;
        case "diagonal":
          // 対角線分割パターン（既存実装例）
          {
            // 対角線で分ける場合は三角形領域を塗り分ける必要があるため、
            // パスを使って描画（ここでは概略のみ記述）
            ctx.beginPath();
            // 左上から右下への対角線を基準に、上側三角形をURL1色、下側三角形をURL2色とする例
            ctx.moveTo(x, y);
            ctx.lineTo(x + cellSize, y);
            ctx.lineTo(x + cellSize, y + cellSize);
            ctx.closePath();
            ctx.fillStyle = bit1 ? "#000" : "#FFF";       // URL1の色
            ctx.fill();  // 上三角形塗り潰し
            ctx.beginPath();
            ctx.moveTo(x, y);
            ctx.lineTo(x, y + cellSize);
            ctx.lineTo(x + cellSize, y + cellSize);
            ctx.closePath();
            ctx.fillStyle = bit2 ? "#000" : "#FFF";       // URL2の色
            ctx.fill();  // 下三角形塗り潰し
          }
          break;
        case "checker-symmetric":
          // 提案するチェッカーシンメトリック4分割パターン
          {
            // 1. 分割する幅と高さを計算（half1が大きい側、half2が小さい側になる）
            let halfW1 = Math.floor(cellSize / 2);
            let halfH1 = Math.floor(cellSize / 2);
            let halfW2 = cellSize - halfW1;
            let halfH2 = cellSize - halfH1;
            // 奇数セルサイズの場合、halfW1・halfH1は切り捨て、halfW2・halfH2が+1された領域になります。
            // もしURL1をデフォルト優先（中心寄せ）にする場合、URL1側領域を大きくする:
            // URL1側が左上になるよう想定し、left/topを大きめに調整
            if (preferURL1) {                // ※preferURL1は必要に応じて設定するフラグ
              halfW1 = Math.ceil(cellSize / 2);  // URL1側（左）の幅を切り上げ
              halfH1 = Math.ceil(cellSize / 2);  // URL1側（上）の高さを切り上げ
              halfW2 = cellSize - halfW1;        // URL2側（右）の幅
              halfH2 = cellSize - halfH1;        // URL2側（下）の高さ
            }
            // 2. 対角に配置する色を決定
            // ここではURL1の色を左上&右下、URL2の色を右上&左下に配置
            const color1 = bit1 ? "#000" : "#FFF";
            const color2 = bit2 ? "#000" : "#FFF";
            // 3. 4つの小矩形領域を塗り分ける
            // 左上小領域 (width=halfW1, height=halfH1)
            ctx.fillStyle = color1;
            ctx.fillRect(x, y, halfW1, halfH1);
            // 右上小領域 (width=halfW2, height=halfH1)
            ctx.fillStyle = color2;
            ctx.fillRect(x + halfW1, y, halfW2, halfH1);
            // 左下小領域 (width=halfW1, height=halfH2)
            ctx.fillStyle = color2;
            ctx.fillRect(x, y + halfH1, halfW1, halfH2);
            // 右下小領域 (width=halfW2, height=halfH2)
            ctx.fillStyle = color1;
            ctx.fillRect(x + halfW1, y + halfH1, halfW2, halfH2);
          }
          break;
      } // switch
    } // if (bit1 === bit2)
  } // for c
} // for r
```

上記のコードでは、`pattern === "checker-symmetric"`の場合に新しい処理を行っています。ポイントをおさらいすると:

* **halfW1, halfH1 と halfW2, halfH2:** それぞれセル内の左領域幅・上領域高さと、右領域幅・下領域高さです。`Math.floor(cellSize/2)`と`cellSize - Math.floor(cellSize/2)`で計算することで、偶数サイズならピクセルを正確に二分割し、奇数サイズなら**一方を1ピクセル大きく**しています。コード中ではさらに`preferURL1`フラグ（必要なら事前に設定）によって、URL1側の領域を`Math.ceil`にすることで大きめに取る例を示しました。これにより**セル中心をURL1側にオフセット**できます（URL2優先の場合は逆にする）。

* **color1 と color2:** それぞれURL1用・URL2用の描画色を決めています。ここでは `bit1`（URL1の該当セルが黒か白か）に応じて`"#000"`または`"#FFF"`をセットしています（`bit1`が真=trueなら黒、偽=falseなら白と仮定）。`color2`も同様です。

* **塗り分け順序:** 左上→右上→左下→右下の順に`fillRect`で塗っています。この順序自体に深い意味はありませんが、重ね順を気にせず4領域を別々に描画するため、**各領域の座標とサイズをミスなく指定すること**が重要です。左上小領域は開始点そのまま、右上はX座標を`x + halfW1`に、左下はY座標を`y + halfH1`に、右下は両方ずらした位置にしています。サイズも対応するhalfW/halfHを使用することで、全4領域がちょうどセル全体を埋めるようになります（ピクセルの重複や抜けがないように注意します）。

* **境界線のアンチエイリアス対策:** `fillRect`で整数座標・整数サイズを指定しているため、このコードではキャンバスにピクセル単位で描画できます。各領域の位置・大きさを整数計算しているので、ブラウザのキャンバス描画でもアンチエイリアスのボケは発生しません（ちょうどピクセルに揃うため）。【※もし分割線を小数点で指定した場合、ブラウザが中間色を描く恐れがあるので、本実装ではMath.floor/ceilで整数化しています。】

* **他のパターンへの影響なし:** switch文で既存のvertical/horizontal/diagonalパターン処理はそのまま残し、新ケースを追加しているので、従来の機能には影響ありません。UI側でパターン選択肢に"checker-symmetric"を加え、この値をpatternとして渡せば、新方式が適用されます。

## 効果と留意点

以上の実装により、新しい**checker-symmetric**パターンでは各セルが微細な市松模様に描かれ、従来より**読み取り角度に対する耐性が向上**します。特に真正面からのスキャン時でも片方のURL（デフォルト優先側）が確実に読み取れるようになり、スマホ等で傾けながらスキャンすることで反対側のURLも容易に取得できるようになります。実験的な研究でも、このようなモジュール分割によるデュアルメッセージQRコードは標準のQRコードリーダーで正常に動作し、垂直・水平・斜めいずれの方向の傾きに対しても安定した結果を示しています。エラー訂正レベルは既存実装同様に最高の**Hレベル**を推奨します。分割によるパターンの攪乱をエラー訂正で補完し、標準規格から外れる試みである本方式でも読み取りを可能にするためです。また、出力画像のモジュールサイズが小さすぎると斜め角度でのサンプリングが不安定になる恐れがあります。可能であればセルあたり**9ピクセル以上**のサイズを確保し、プリンタで出力する場合もドットが潰れない解像度を維持してください。

最後に、UI側では"checker-symmetric"パターンを選択可能にし、他のvertical/horizontal/diagonalパターンも従来通り保持します。コード上の変更は局所的で、既存の描画ループに上記スニペットを組み込むだけで動作します。修正後のQRコード生成結果をぜひテストし、読み取りやすさが向上していることを確認してください。これにより、ユーザーは**1つのQRコードで2つのURLをより確実に使い分けられる**ユニークな体験を得られるでしょう。

**参考文献・情報源:** 専門家の実験によれば、本方式は標準QRコードリーダーで角度に応じ異なる内容を表示させることに成功しており、ISO規格の想定を超える応用例として報告されています。こうした最新知見も踏まえつつ、安全面では誤認識のリスクも考慮してください。本改良が御社のアプリケーションにおけるデュアルQRコードの信頼性向上に寄与することを願っています。

Hackaday: *"The trick is to identify the cells that are different ... split them in half vertically and horizontally: making them into a tiny checkerboard. It appears that some QR decoders sample in the center of each target square, and the center will be in one side or the other depending on the tilt..."* （角度による中心サンプリング位置の偏りを利用してセルを縦横二分割し、市松模様にする手法の説明）

Dual QR Code README: *"When two QR codes have different patterns at the same position, the cell is split in half ... When both QR codes have the same pattern ... the cell is filled with a solid color. Due to the high error correction ... QR code scanners can still read either URL depending on the scanning angle."* （2つのQRコードのパターン合成概要と、誤り訂正レベルHによる読取の可能性に関する言及）

Noever et al., *Dueling QR Codes (2025)*: *"...reliable dual-message decoding using standard QR readers at module scales of 9-11 pixels, with successful angle-dependent reading demonstrated across vertical, horizontal, and diagonal orientations."* （標準のQRリーダーでデュアルメッセージを安定復号するにはモジュールあたり9～11ピクセル程度の解像度が効果的であること、垂直・水平・斜め方向の傾きに対して成功した実験結果）


# QRコード「checker-symmetric」分割方式 解説図

下記では、セル分割アルゴリズムの全体フローと各セル内の市松模様分割手順を**図解**形式で示します。

---

## 1. 全体フロー

```
+---------------------+      +---------------------+      +-----------------------+
| QR1, QR2 データ生成 | ---> | セルごとに比較      | ---> | 同一 or 相違 の判定     |
+---------------------+      +---------------------+      +-----------------------+
                                                          |
                              +---------------------------+-----------------------------+
                              |                                                         |
                     +----------------------+                                  +-----------------------+
                     | 同一セル (bit1==bit2) |                                  | 相違セル (bit1!=bit2)  |
                     +----------+-----------+                                  +-----------+-----------+
                                |                                                        |
                +---------------v----------------+                                 +----------------v----------------+
                | セル全体を単色 (#000 or #FFF)  |                                 | checker-symmetric 分割描画     |
                +--------------------------------+                                 +--------------------------------+
```

---

## 2. 相違セルの細分化イメージ

```
+------------------------------+    セル全体 (cellSize×cellSize)
|                              |    をさらに 2×2 の小領域に分割
|                              |    (縦: halfH1, halfH2 / 横: halfW1, halfW2)
|                              |
+------------------------------+
         ↙ halfW1  ↘ halfW2
         ↙ halfH1  ↘ halfH1

+------------+------------+
| 左上 (W1×H1) | 右上 (W2×H1) |
+------------+------------+ halfH1
| 左下 (W1×H2) | 右下 (W2×H2) |
+------------+------------+
           ↑ halfH2
           ↑ ↖ halfW1 ↗
```

---

## 3. 色配置の対角パターン

全ての相違セルで **左上 & 右下** を URL1 色、**右上 & 左下** を URL2 色で塗り分けます。

```
┌──────────┬──────────┐
│  URL1    │  URL2    │
│ (L=黒)   │ (R=白)   │ <- 小領域1 (左上)
├──────────┼──────────┤
│  URL2    │  URL1    │ <- 小領域4 (右下)
└──────────┴──────────┘
  ↑        ↑           
  ↑小領域3  ↑小領域2  ↗
```

---

## 4. セル中心オフセットによる優先領域

* `half1 = Math.ceil(cellSize/2)` を URL1 側領域に適用し、
* セル中心が必ず URL1 色側に含まれるよう調整

```
cellSize = 11px の場合:
 half1 = ceil(11/2) = 6px
 half2 = 11 - 6 = 5px

+------------+<-- 6px ----+   <-- 左上/右下 が大きい領域 (URL1 優先)
|            |             |
+------------+-------------+  6px 高さ
|            |             |
+------------+-------------+  5px 高さ
```

---

## 5. 機能パターンの扱い

```
┌──┬──────────────┬─────┐
│  │ 機能パターン 　│    │  ← 位置検出&タイミング等の重要部分は分割せず
├──┴──────────────┴─────┤     片方の色でそのまま描画
│      データセル        │
│   (checker-symmetric) │
└───────────────────────┘
```

---

以上の図解を参考に、`App.jsx`のキャンバス描画ループに**checker-symmetric**分割ロジックを追加してください。
