# DualQRCode プロジェクト解説

## プロジェクト概要

**DualQRCode**は、1つのQRコードに2つの異なるURLを埋め込むことができる実験的なReactアプリケーションです。QRコードを異なる角度からスキャンすると、異なるURLが表示される仕組みになっています。

**⚠️ 重要な注意事項**: このプロジェクトは実験的なコードであり、QRコード標準に反し、標準を破壊するものです。実際のアプリケーションでは絶対に使用すべきではありません。あくまで概念実証（Proof of Concept）として提供されています。

## 技術スタック

### 主要技術
- **React 18.3.1** - フロントエンドフレームワーク
- **Vite 6.0.5** - ビルドツール・開発サーバー
- **qrcode 1.5.4** - QRコード生成ライブラリ
- **jimp 1.6.0** - 画像処理ライブラリ

### 開発ツール
- **ESLint** - コード品質管理
- **TypeScript型定義** - 型安全性の向上

## プロジェクト構造

```
DualQRCode-main/
├── public/                 # 静的ファイル
│   ├── qr-icon.svg        # QRコードアイコン
│   └── vite.svg           # Viteロゴ
├── src/                   # ソースコード
│   ├── App.jsx           # メインアプリケーションコンポーネント
│   ├── App.css           # アプリケーションスタイル
│   ├── main.jsx          # アプリケーションエントリーポイント
│   ├── index.css         # グローバルスタイル
│   └── assets/           # アセットファイル
├── package.json          # 依存関係とスクリプト定義
├── vite.config.js        # Vite設定
├── eslint.config.js      # ESLint設定
├── index.html            # HTMLテンプレート
└── README.md             # プロジェクト説明
```

## 核心機能の詳細解説

### 1. デュアルQRコード生成アルゴリズム

`src/App.jsx`の`generateDualQRCode`関数が核心部分です：

```javascript
const generateDualQRCode = async () => {
  // 2つのURLからQRコードデータを生成
  const qr1Data = await QRCode.create(url1, {
    errorCorrectionLevel: 'H',  // 高エラー訂正レベル
    version: qrVersion
  })
  const qr2Data = await QRCode.create(url2, {
    errorCorrectionLevel: 'H',
    version: qrVersion
  })
  
  // ピクセル分割パターンで2つのQRコードを結合
  // ...
}
```

### 2. ピクセル分割パターン

3つの分割パターンが実装されています：

1. **垂直分割（Vertical）**: セルを左右に分割
2. **水平分割（Horizontal）**: セルを上下に分割  
3. **対角線分割（Diagonal）**: セルを対角線で分割

```javascript
if (splitPattern === 'diagonal') {
  // 対角線分割パターン
  ctx.fillStyle = cell1 ? '#000000' : '#FFFFFF'
  ctx.beginPath()
  ctx.moveTo(x, y)
  ctx.lineTo(x + cellSize, y)
  ctx.lineTo(x + cellSize, y + cellSize)
  ctx.fill()
  
  ctx.fillStyle = cell2 ? '#000000' : '#FFFFFF'
  ctx.beginPath()
  ctx.moveTo(x, y)
  ctx.lineTo(x, y + cellSize)
  ctx.lineTo(x + cellSize, y + cellSize)
  ctx.fill()
}
```

### 3. エラー訂正レベルHの活用

QRコードの高エラー訂正レベル（H）を利用することで、一部のピクセルが変更されてもQRコードリーダーが正しく読み取れるようになっています。これにより、分割されたピクセルパターンでもスキャンが可能になります。

## 主要コンポーネント

### App.jsx - メインコンポーネント

**状態管理**:
- `url1`, `url2`: 入力された2つのURL
- `qrCodeData`: 生成されたQRコードの画像データ
- `splitPattern`: 選択された分割パターン
- `qrVersion`: QRコードのバージョン
- `invertUrls`: URLの反転フラグ

**主要機能**:
1. **QRコード生成**: 2つのURLからデュアルQRコードを生成
2. **ピクセル分割**: 3つのパターンでピクセルを分割
3. **高解像度出力**: スケールファクター2で高品質画像を生成
4. **エラーハンドリング**: 生成エラーの適切な処理

### スタイリング（App.css）

**レスポンシブデザイン**:
- モバイル対応のメディアクエリ
- フレキシブルレイアウト
- タッチフレンドリーなUI

**インタラクティブ要素**:
- オプションの折りたたみ機能
- ホバーエフェクト
- スムーズなアニメーション

## 設定ファイル

### package.json
```json
{
  "name": "doubleqrcode3",
  "version": "0.0.0",
  "dependencies": {
    "jimp": "^1.6.0",
    "qrcode": "^1.5.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
```

### vite.config.js
```javascript
export default defineConfig({
  plugins: [react()],
  base: './',  // 相対パスベース
  build: {
    outDir: 'dist',
    assetsDir: 'assets'
  }
})
```

## 使用方法

1. **URL入力**: 2つのURLを入力フィールドに入力
2. **オプション設定**: 
   - ピクセル分割パターンを選択
   - QRコードバージョンを選択（v2-v10）
   - URL反転オプションを設定
3. **生成**: "Generate QR Code"ボタンをクリック
4. **スキャン**: 異なる角度からQRコードをスキャン

## 技術的制限と注意事項

### 制限事項
- QRコード標準に準拠していない
- スキャン結果は角度に依存
- 2番目のURLが優先される傾向
- すべてのQRコードリーダーで動作する保証なし

### セキュリティ考慮事項
- クライアントサイドでの処理
- データ収集なし
- ローカルでの完全処理

## 開発環境のセットアップ

```bash
# 依存関係のインストール
npm install

# 開発サーバーの起動
npm run dev

# ビルド
npm run build

# リント
npm run lint
```

## ライセンスとクレジット

- **インスピレーション**: Christian Walther
- **ライセンス**: プロジェクト固有のライセンス
- **警告**: 実験的コードのため実用禁止

## まとめ

DualQRCodeは、QRコード標準の制限を超えた実験的なプロジェクトです。高エラー訂正レベルを活用して2つのURLを1つのQRコードに埋め込む技術的デモンストレーションとして、QRコード技術の可能性を探求しています。ただし、実用性よりも教育的・研究的価値に重点を置いたプロジェクトであることに注意が必要です。 